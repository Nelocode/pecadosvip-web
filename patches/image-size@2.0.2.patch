diff --git a/dist/detector.cjs b/dist/detector.cjs
index 0898608dbeca36722dfd795341b1c3c1448f58aa..724e2f3ce8d999b523fca98408b81680a5506b40 100644
--- a/dist/detector.cjs
+++ b/dist/detector.cjs
@@ -30,11 +30,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
@@ -251,9 +252,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/detector.mjs b/dist/detector.mjs
index 67afcab02d627f95e98c938499e07a934b832a26..66b54263ae3ada2c0fa0dac50d0efbb459f0df01 100644
--- a/dist/detector.mjs
+++ b/dist/detector.mjs
@@ -28,11 +28,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
@@ -249,9 +250,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/fromFile.cjs b/dist/fromFile.cjs
index c226d3dac07f235db456c774b26cca88f655e1af..07a37a5e6ff993cef497e91947138737b1ce92f1 100644
--- a/dist/fromFile.cjs
+++ b/dist/fromFile.cjs
@@ -56,11 +56,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
@@ -277,9 +278,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/fromFile.mjs b/dist/fromFile.mjs
index 4a3c44432982397204f7a9d04fc66ef65a8e7ba4..b92be004fdc582200cee1ab0e9dcdec1d33f3c94 100644
--- a/dist/fromFile.mjs
+++ b/dist/fromFile.mjs
@@ -33,11 +33,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
@@ -254,9 +255,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/index.cjs b/dist/index.cjs
index bde0210eb131db06ccca30a5c466c7252a03af50..601068a4adf6b2df297e0132ecbd947b0ad8008d 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -32,11 +32,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
@@ -253,9 +254,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/index.mjs b/dist/index.mjs
index 22c17afad4e406eba013fff7858366cb5f8e3ba4..bf4acc09529df8424581035081018cce8f3b4373 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -28,11 +28,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
@@ -249,9 +250,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/lookup.cjs b/dist/lookup.cjs
index 9731ad8bb0949f8fdf9297851bdb312e91282f01..385a2e095981b6dc0014e757920b9d9510c0b529 100644
--- a/dist/lookup.cjs
+++ b/dist/lookup.cjs
@@ -30,11 +30,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
@@ -251,9 +252,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/lookup.mjs b/dist/lookup.mjs
index f787d1c8408d7b681fec8617595d058f66959869..8e79e399a244335f4fb4213f6acf79f7b54af4e4 100644
--- a/dist/lookup.mjs
+++ b/dist/lookup.mjs
@@ -28,11 +28,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
@@ -249,9 +250,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/types/heif.cjs b/dist/types/heif.cjs
index 1f490788ea654ea9896060a5c0253d81e348af92..f533b6c024d38ed01ffc66ed4d78c7b75831aba3 100644
--- a/dist/types/heif.cjs
+++ b/dist/types/heif.cjs
@@ -8,11 +8,12 @@ var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, fa
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
diff --git a/dist/types/heif.mjs b/dist/types/heif.mjs
index 01330919e01b3c034a96a7b4b1dbf5a7f658bb2d..acb58bd6944506974644ddda1da1902d8b8684b9 100644
--- a/dist/types/heif.mjs
+++ b/dist/types/heif.mjs
@@ -6,11 +6,12 @@ var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, fa
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
diff --git a/dist/types/icns.cjs b/dist/types/icns.cjs
index 32c8d370dcf80a10f14a6deebfdbe04d3c70585c..e5ad2c8fdf280d5571dd3975e942e263daf48d10 100644
--- a/dist/types/icns.cjs
+++ b/dist/types/icns.cjs
@@ -72,9 +72,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/types/icns.mjs b/dist/types/icns.mjs
index 8710027bc5890a79d05a7fdb87de3018e556729e..80c4b237403fb8d515bb4ad24fad18d5d6a0fc4f 100644
--- a/dist/types/icns.mjs
+++ b/dist/types/icns.mjs
@@ -70,9 +70,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/types/index.cjs b/dist/types/index.cjs
index 6949cd134db7ab59dae7b804b36c7bacb4e387cf..65e44fa463f1752699f566e3111e6657dd6fb4ab 100644
--- a/dist/types/index.cjs
+++ b/dist/types/index.cjs
@@ -30,11 +30,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
@@ -251,9 +252,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/types/index.mjs b/dist/types/index.mjs
index b026dda4aa16c6077a76e4b3cfe3f54fe1d6d1d0..04450b13fd51833ae0766f13108085be7c4af4cf 100644
--- a/dist/types/index.mjs
+++ b/dist/types/index.mjs
@@ -28,11 +28,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
@@ -249,9 +250,13 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (imageLength < 8 || imageOffset + imageLength > Math.min(fileLength, inputLength)) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/types/jp2.cjs b/dist/types/jp2.cjs
index cdd7c884efeb940b99f69a8f1d6ed361a1979211..5d307174bdb9c7d2d8f041c7cd5dcf96d76e59b7 100644
--- a/dist/types/jp2.cjs
+++ b/dist/types/jp2.cjs
@@ -8,11 +8,12 @@ var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, fa
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
diff --git a/dist/types/jp2.mjs b/dist/types/jp2.mjs
index 63a537635b4f91250431a53225d4521ed60cf67d..944f95c2b87bf3c81afa3b3c586e4a5a7f2ac1eb 100644
--- a/dist/types/jp2.mjs
+++ b/dist/types/jp2.mjs
@@ -6,11 +6,12 @@ var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, fa
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
diff --git a/dist/types/jxl.cjs b/dist/types/jxl.cjs
index a12095be85c7a8fd25763895a47c46b4292ebd9c..0b0d8a849a6116e726d41b015db5be3e2ecca47b 100644
--- a/dist/types/jxl.cjs
+++ b/dist/types/jxl.cjs
@@ -49,11 +49,12 @@ var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, fa
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
diff --git a/dist/types/jxl.mjs b/dist/types/jxl.mjs
index 49b0c4cac6abb366378e5feed745f16981b441c0..3ed397ef5c9fd39279c52310073869b3d87e6a05 100644
--- a/dist/types/jxl.mjs
+++ b/dist/types/jxl.mjs
@@ -47,11 +47,12 @@ var readUInt32BE = (input, offset = 0) => getView(input, offset).getUint32(0, fa
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
diff --git a/dist/types/utils.cjs b/dist/types/utils.cjs
index 23d52e4cbf271e667feb2d7f040c07f69678d3aa..05d51ade83fc3a8b77f3002cac91f75e03ee9775 100644
--- a/dist/types/utils.cjs
+++ b/dist/types/utils.cjs
@@ -30,11 +30,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
diff --git a/dist/types/utils.mjs b/dist/types/utils.mjs
index 3f09dceb036e40be8538dbb5588d88c951339158..b412bc849376274e772593cc7399d8e48947e6e4 100644
--- a/dist/types/utils.mjs
+++ b/dist/types/utils.mjs
@@ -28,11 +28,12 @@ function readUInt(input, bits, offset = 0, isBigEndian = false) {
 function readBox(input, offset) {
   if (input.length - offset < 4) return;
   const boxSize = readUInt32BE(input, offset);
-  if (input.length - offset < boxSize) return;
+  const normalizedBoxSize = boxSize === 0 ? input.length - offset : boxSize;
+  if (normalizedBoxSize < 8 || input.length - offset < normalizedBoxSize) return;
   return {
     name: toUTF8String(input, 4 + offset, 8 + offset),
     offset,
-    size: boxSize
+    size: normalizedBoxSize
   };
 }
 function findBox(input, boxName, currentOffset) {
