# Hero sintético del preview local

Este directorio conserva el activo editorial usado exclusivamente en la portada local sintética de PecadosVip.

- `master/` conserva el PNG original generado con IA.
- `selected/` contiene el WebP optimizado que entrega el middleware local.
- El recurso no está dentro de `public/`, no se sirve en producción y no autoriza publicación.
- La imagen representa una identidad adulta completamente ficticia. No corresponde a una modelo real ni acredita disponibilidad comercial.

La revisión humana, comercial y legal final permanece pendiente.
